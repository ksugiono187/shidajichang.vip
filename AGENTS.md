# AGENTS.md - 多 Agent 协作与安全规范

## 协作分工
- **Antigravity**：主要负责 UI 微调、视觉优化、配色卡片优化及前端细节打磨。
- **Claude Code**：主要负责项目架构、组件拆分、TypeScript 类型与 Bug 修复。

## 协作规则
1. **专注指定文件**：多个 AI 工具协作时，只修改任务指定文件，避免互相覆盖。
2. **禁止无关重构**：保持既有代码稳固，不要重构无关模块。
3. **隔离安全边界**：严禁读取密钥和私密文件。
4. **单文件并发控制**：不要同时让多个 Agent 修改同一个文件。
5. **流程一致性**：修改前列出文件，修改后只总结变更。
