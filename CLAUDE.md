# CLAUDE.md - 项目维护规范

## 核心原则
- **项目定位**：高级中文 Astro SEO 博客与品牌落地页内容站框架。
- **SEO 优先**：保持 Semantic HTML、合理的 H1/H2 结构、Title/Description 规范与静态导出速度。
- **UI 质量**：保持高级、干净、科技感与移动端友好，避免低质样式。

## 行为约束
1. **防全盘扫描**：不要全仓库乱扫，定位到具体文件进行操作。
2. **防隐私泄露**：不要读取 `.env`、账号、密码、cookies、token、密钥或私密文件。
3. **文件操作限制**：每次任务最多修改 1-5 个文件（除非用户明确要求搭建整体架构）。
4. **排除目录**：切勿修改或删除 `node_modules`、`dist`、`.astro`、`.git`。
5. **依赖管控**：严禁安装无用的大型 UI 框架、后端系统、数据库或无关动画库。
6. **精简内容**：不要生成大量重复真实文章，保持静态生成干净快速。
7. **输出规范**：修改前先列出文件清单，修改后仅输出简短摘要。
